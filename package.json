{
  "name": "rollup-starter-code-splitting",
  "devDependencies": {
    "npm-run-all": "^4.1.2",
    "rollup": "^0.57.1",
    "rollup-plugin-svelte": "^4.0.0",
    "svelte": "^1.58.2"
  },
  "dependencies": {
    "serve": "^6.5.3"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "dev": "npm-run-all --parallel start watch",
    "start": "serve public"
  }
}
